import { FilterTransfer } from '../components/FilterTransfer'
import { SectionBody } from '../style/HomeStyle'

export const Home = () => {
    return <SectionBody>
        <FilterTransfer />
        <FilterTransfer />
    </SectionBody>
    
}